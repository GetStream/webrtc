# Package WebRTC artifacts. Same path as today's Publish so a PR can dispatch it.
# Does not create a GitHub release.

name: Package

run-name: >-
  Package config:${{ inputs.config }}${{ inputs.ios == 'true' && ' iOS' || '' }}${{ inputs.macos == 'true' && (inputs.ios == 'true' && ', macOS' || ' macOS') || '' }}${{ inputs.android == 'true' && ((inputs.ios == 'true' || inputs.macos == 'true') && ', Android' || ' Android') || '' }}${{ inputs.windows == 'true' && ((inputs.ios == 'true' || inputs.macos == 'true' || inputs.android == 'true') && ', Windows' || ' Windows') || '' }}

permissions:
  contents: read

on:
  workflow_dispatch:
    inputs:
      webrtc_ref:
        description: Branch, tag, or SHA for this repo (GetStream/webrtc)
        required: true
        default: main
      ios:
        description: Package iOS xcframework
        type: boolean
        default: true
      macos:
        description: Package macOS xcframework
        type: boolean
        default: true
      android:
        description: Package Android AAR
        type: boolean
        default: true
      windows:
        description: Package Windows libs (fails clearly if the runner is not a WebRTC host)
        type: boolean
        default: false
      config:
        description: GN configuration
        type: choice
        options:
          - release
          - debug
        default: release
      android_arch:
        description: Optional Android ABI, for example arm64-v8a. Empty = default ABI set.
        required: false
        default: ""
      ignore_cache:
        description: Skip restoring dependency caches and force a fresh sync
        type: boolean
        default: false

jobs:
  package:
    uses: ./.github/workflows/_make.yml
    secrets: inherit
    with:
      mode: package
      webrtc_ref: ${{ inputs.webrtc_ref }}
      ios: ${{ inputs.ios }}
      macos: ${{ inputs.macos }}
      android: ${{ inputs.android }}
      windows: ${{ inputs.windows }}
      config: ${{ inputs.config }}
      ignore_cache: ${{ inputs.ignore_cache }}
      android_arch: ${{ inputs.android_arch }}
